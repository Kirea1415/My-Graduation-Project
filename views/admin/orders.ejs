<div class="admin-orders">
  <div class="orders-header">
    <h1><svg class="icon-cart" viewBox="0 0 576 512" width="28" height="28" aria-hidden="true" style="vertical-align:middle;margin-right:8px;"><path d="M551.991 64H144.28l-8.726-44.608C133.35 8.128 123.478 0 112 0H12C5.373 0 0 5.373 0 12v24c0 6.627 5.373 12 12 12h80.24l69.594 355.701C150.796 415.201 144 430.802 144 448c0 35.346 28.654 64 64 64s64-28.654 64-64a63.681 63.681 0 0 0-8.583-32h145.167a63.681 63.681 0 0 0-8.583 32c0 35.346 28.654 64 64 64 35.346 0 64-28.654 64-64 0-18.136-7.556-34.496-19.676-46.142l1.035-4.757c3.254-14.96-8.142-29.101-23.452-29.101H203.76l-9.39-48h312.405c11.29 0 21.054-7.869 23.452-18.902l45.216-208C578.695 78.139 567.299 64 551.991 64zM208 472c-13.234 0-24-10.766-24-24s10.766-24 24-24 24 10.766 24 24-10.766 24-24 24zm256 0c-13.234 0-24-10.766-24-24s10.766-24 24-24 24 10.766 24 24-10.766 24-24 24zm23.438-200H184.98l-31.31-160h368.548l-34.78 160z" fill="currentColor"></path></svg> Lịch sử giao dịch</h1>
    <p class="header-subtitle">Xem giỏ hàng và đơn hàng của người dùng</p>
  </div>

  <% if (typeof flash !== 'undefined' && flash.success && flash.success.length) { %>
    <div class="success-message">
      <%= flash.success.join('<br/>') %>
    </div>
  <% } %>

  <% if (typeof flash !== 'undefined' && flash.error && flash.error.length) { %>
    <div class="error-message">
      <%= flash.error.join('<br/>') %>
    </div>
  <% } %>


  <!-- Filters -->
  <div class="orders-filters">
    <form method="GET" action="/admin/orders" class="filter-form">
      <select name="status" class="filter-select">
        <option value="">Tất cả trạng thái</option>
        <option value="pending" <%= status === 'pending' ? 'selected' : '' %>>Chờ thanh toán</option>
        <option value="paid" <%= status === 'paid' ? 'selected' : '' %>>Đã thanh toán</option>
        <option value="completed" <%= status === 'completed' ? 'selected' : '' %>>Hoàn thành</option>
        <option value="cancelled" <%= status === 'cancelled' ? 'selected' : '' %>>Đã hủy</option>
        <option value="failed" <%= status === 'failed' ? 'selected' : '' %>>Thất bại</option>
      </select>
      <button type="submit" class="btn primary">
        <svg viewBox="0 0 512 512" width="16" height="16" style="display: inline-block; vertical-align: middle; margin-right: 6px;">
          <path d="M479.968 0H32.038C3.613 0-10.729 34.487 9.41 54.627L192 237.255V424a31.996 31.996 0 0 0 10.928 24.082l64 55.983c20.438 17.883 53.072 3.68 53.072-24.082V237.255L502.595 54.627C522.695 34.528 508.45 0 479.968 0zM288 224v256l-64-56V224L32 32h448L288 224z" fill="currentColor"></path>
        </svg>
        Lọc
      </button>
      <% if (status) { %>
        <a href="/admin/orders" class="btn secondary">✕ Xóa bộ lọc</a>
      <% } %>
    </form>
  </div>

  <div class="orders-table-wrapper">
    <% if (!orders || orders.length === 0) { %>
      <div class="empty-state">
        <p>Không tìm thấy đơn hàng nào</p>
      </div>
    <% } else { %>
      <table class="orders-table">
        <thead>
          <tr>
            <th>ID</th>
            <th>Khách hàng</th>
            <th>Sản phẩm</th>
            <th>Tổng tiền</th>
            <th>Trạng thái</th>
            <th>Phương thức</th>
            <th>Ngày tạo</th>
            <th>Ghi chú</th>
          </tr>
        </thead>
        <tbody>
          <% orders.forEach(order => { %>
            <tr>
              <td>#<%= order.id %></td>
              <td>
                <div class="user-info">
                  <div class="user-name"><%= order.user_name || 'N/A' %></div>
                  <div class="user-email"><%= order.user_email || 'N/A' %></div>
                </div>
              </td>
              <td>
                <div class="order-items-preview">
                  <% if (order.items && order.items.length > 0) { %>
                    <% order.items.slice(0, 2).forEach(item => { %>
                      <div class="item-preview"><%= item.product_title %> × <%= item.quantity %></div>
                    <% }) %>
                    <% if (order.items.length > 2) { %>
                      <div class="item-preview-more">+<%= order.items.length - 2 %> sản phẩm khác</div>
                    <% } %>
                  <% } else { %>
                    <span class="muted">Không có sản phẩm</span>
                  <% } %>
                </div>
              </td>
              <td class="right">
                <strong><%= ((order.total_cents || 0) / 100).toLocaleString('vi-VN', {style:'currency',currency:'VND'}) %></strong>
              </td>
              <td>
                <span class="status-badge status-<%= order.status %>">
                  <%= order.status === 'paid' ? '✅ Đã thanh toán' : 
                      order.status === 'completed' ? '✅ Hoàn thành' :
                      order.status === 'pending' ? '⏳ Chờ thanh toán' :
                      order.status === 'cancelled' ? '❌ Đã hủy' :
                      order.status === 'failed' ? '❌ Thất bại' : order.status %>
                </span>
              </td>
              <td><%= order.payment_method || '-' %></td>
              <td><%= new Date(order.created_at).toLocaleDateString('vi-VN') %></td>
              <td>
                <% if (order.user_deleted_at) { %>
                  <span class="user-deleted-badge" title="Người dùng đã xóa: <%= new Date(order.user_deleted_at).toLocaleString('vi-VN') %>">
                    🗑️ Đã xóa bởi user
                  </span>
                <% } else { %>
                  <span class="muted">-</span>
                <% } %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>

      <% if (totalPages > 1) { %>
        <div class="pagination">
          <% if (page > 1) { %>
            <a href="?page=<%= page - 1 %><%= status ? '&status=' + encodeURIComponent(status) : '' %>" class="pagination-btn">← Trước</a>
          <% } %>
          <span class="pagination-info">Trang <%= page %> / <%= totalPages %></span>
          <% if (page < totalPages) { %>
            <a href="?page=<%= page + 1 %><%= status ? '&status=' + encodeURIComponent(status) : '' %>" class="pagination-btn">Sau →</a>
          <% } %>
        </div>
      <% } %>
    <% } %>
  </div>
</div>

<style>
.admin-orders {
  max-width: 1400px;
  margin: 0 auto;
  padding: 20px;
}

.orders-header {
  margin-bottom: 30px;
  padding-bottom: 20px;
  border-bottom: 2px solid var(--border);
}

.orders-header h1 {
  margin: 0 0 8px 0;
  font-size: 32px;
  color: var(--fg);
}

.header-subtitle {
  margin: 0;
  color: var(--muted);
  font-size: 16px;
}

.revenue-stats {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 16px;
  margin-bottom: 24px;
}

.revenue-stats .stat-card {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 20px;
  text-align: center;
}

.revenue-stats .stat-card.highlight {
  border-color: var(--green);
  background: rgba(22, 163, 74, 0.05);
}

.revenue-stats .stat-label {
  color: var(--muted);
  font-size: 14px;
  margin-bottom: 8px;
}

.revenue-stats .stat-value {
  font-size: 24px;
  font-weight: 700;
  color: var(--fg);
}

.orders-filters {
  margin-bottom: 20px;
}

.filter-form {
  display: flex;
  gap: 12px;
  align-items: center;
}

.filter-select {
  padding: 12px 16px;
  border: 1px solid var(--border);
  border-radius: 8px;
  background: var(--card);
  color: var(--fg);
  font-size: 14px;
}

.orders-table-wrapper {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 12px;
  overflow: hidden;
}

.orders-table {
  width: 100%;
  border-collapse: collapse;
}

.orders-table thead {
  background: rgba(255, 255, 255, 0.02);
}

.orders-table th {
  padding: 16px;
  text-align: left;
  font-weight: 600;
  color: var(--muted);
  font-size: 14px;
  border-bottom: 2px solid var(--border);
}

.orders-table th.right {
  text-align: right;
}

.orders-table td {
  padding: 16px;
  border-bottom: 1px solid var(--border);
  color: var(--fg);
  font-size: 14px;
}

.orders-table td.right {
  text-align: right;
}

.orders-table tbody tr:hover {
  background: rgba(255, 255, 255, 0.02);
}

.user-info {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.user-name {
  font-weight: 600;
  color: var(--fg);
}

.user-email {
  font-size: 12px;
  color: var(--muted);
}

.order-items-preview {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.item-preview {
  font-size: 13px;
  color: var(--fg);
}

.item-preview-more {
  font-size: 12px;
  color: var(--muted);
  font-style: italic;
}

.status-badge {
  display: inline-block;
  padding: 4px 12px;
  border-radius: 12px;
  font-size: 12px;
  font-weight: 600;
}

.status-badge.status-paid,
.status-badge.status-completed {
  background: rgba(22, 163, 74, 0.2);
  color: #10b981;
}

.status-badge.status-pending {
  background: rgba(245, 158, 11, 0.2);
  color: #f59e0b;
}

.status-badge.status-cancelled,
.status-badge.status-failed {
  background: rgba(239, 68, 68, 0.2);
  color: #ef4444;
}

.btn-view {
  padding: 6px 12px;
  background: var(--green);
  color: white;
  text-decoration: none;
  border-radius: 6px;
  font-size: 12px;
  font-weight: 600;
  transition: all 0.2s;
}

.btn-view:hover {
  background: #16a34a;
}

.empty-state {
  padding: 60px 20px;
  text-align: center;
  color: var(--muted);
}

.pagination {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 16px;
  padding: 20px;
  border-top: 1px solid var(--border);
}

.pagination-info {
  color: var(--muted);
  font-size: 14px;
}

.pagination-btn {
  padding: 8px 16px;
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 8px;
  color: var(--fg);
  text-decoration: none;
  font-size: 14px;
  transition: all 0.2s;
}

.pagination-btn:hover {
  background: rgba(22, 163, 74, 0.1);
  border-color: var(--green);
  color: var(--green);
}

@media (max-width: 968px) {
  .orders-table {
    font-size: 12px;
  }
  
  .orders-table th,
  .orders-table td {
    padding: 12px 8px;
  }
}
</style>

